apiVersion: v1
kind: Service
metadata:
    name: laurentgrangeauazuredevopsdevoxxma
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: laurentgrangeauazuredevopsdevoxxma